{
  "name": "subtitle-tools",
  "version": "1.0.0",
  "private": true,
  "description": "SubtitleKit — client-side subtitle converter, fixer and caption QC toolkit (static site, deployed to GitHub Pages)",
  "scripts": {
    "build": "node build/gen.js --base https://aiharryone.github.io/subtitle-tools --out dist",
    "test": "node tests/test-core.js",
    "test:e2e": "py -X utf8 tests/browser_test.py",
    "deploy": "node build/deploy.js"
  },
  "license": "MIT",
  "engines": { "node": ">=18" }
}
